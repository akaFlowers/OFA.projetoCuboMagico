@model IEnumerable<projetoCuboMagico.Models.Gerente>

@{
    ViewData["Title"] = "Gerentes";
}

<div class="page-header">
    <h2>@ViewData["Title"]</h2>
</div>
<div style="padding-bottom: 15px;" class="text-right">
    @Html.ActionLink("Cadastrar", "Create", null, new { @class = "btn btn-success" })
</div>
<div class="table-responsive">
    @if (Model.Any())
    {
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(g => g.Id)</th>
                    <th>@Html.DisplayNameFor(g => g.NomeCompleto)</th>
                    <th>@Html.DisplayNameFor(g => g.DataNascimento)</th>
                    <th>@Html.DisplayNameFor(g => g.Sexo)</th>
                    <th>@Html.DisplayNameFor(g => g.Cpf)</th>
                    <th>@Html.DisplayNameFor(g => g.Email)</th>
                    <th>@Html.DisplayNameFor(g => g.Telefone)</th>
                    <th>@Html.DisplayNameFor(g => g.Celular)</th>
                    <th>@Html.DisplayNameFor(g => g.Endereco)</th>
                    <th>@Html.DisplayNameFor(g => g.IdUsuario)</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var gerente in Model)
                {
                <tr>
                    <td>@Html.DisplayFor(g => gerente.Id)</td>
                    <td>@Html.DisplayFor(f => gerente.NomeCompleto)</td>
                    <td>@Html.DisplayFor(f => gerente.DataNascimento)</td>
                    <td>@Html.DisplayFor(f => gerente.Sexo)</td>
                    <td>@Html.DisplayFor(f => gerente.Cpf)</td>
                    <td>@Html.DisplayFor(f => gerente.Email)</td>
                    <td>@Html.DisplayFor(f => gerente.Telefone)</td>
                    <td>@Html.DisplayFor(f => gerente.Celular)</td>
                    <td>@Html.DisplayFor(f => gerente.Endereco)</td>
                    <td>@Html.DisplayFor(f => gerente.IdUsuario)</td>

                    <td>
                        @Html.ActionLink("Detalhes", "Details", new { id = gerente.Id }, new { @class = "btn btn-info" })
                        @Html.ActionLink("Editar", "Edit", new { id = gerente.Id }, new { @class = "btn btn-warning" })
                        @Html.ActionLink("Deletar", "Delete", new { id = gerente.Id }, new { @class = "btn btn-danger" })
                    </td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-danger">
            <p>Não há Gerentes cadastrados.</p>
        </div>
    }
</div>