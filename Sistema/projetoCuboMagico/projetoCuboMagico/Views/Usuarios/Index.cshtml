@model IEnumerable<projetoCuboMagico.Models.Usuario>

@{
    ViewData["Title"] = "Usuarios";
}

<div class="page-header">
    <h2>@ViewData["Title"]</h2>
</div>
<div style="padding-bottom: 15px;" class="text-right">
    @Html.ActionLink("Cadastrar", "Create", null, new { @class = "btn btn-success" })
</div>
<div class="table-responsive">
    @if (Model.Any())
    {
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(u => u.Id)</th>
                    <th>@Html.DisplayNameFor(u => u.Usuarioo)</th>
                    <th>@Html.DisplayNameFor(u => u.Senha)</th>
                    <th>@Html.DisplayNameFor(u => u.NivelAcesso)</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var usuario in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(u => usuario.Id)</td>
                        <td>@Html.DisplayFor(u => usuario.Usuarioo)</td>
                        <td>@Html.DisplayFor(u => usuario.Senha)</td>
                        <td>@Html.DisplayFor(u => usuario.NivelAcesso)</td>
                        <td>
                            @Html.ActionLink("Detalhes", "Details", new { id = usuario.Id }, new { @class = "btn btn-info" })
                            @Html.ActionLink("Editar", "Edit", new { id = usuario.Id }, new { @class = "btn btn-warning" })
                            @Html.ActionLink("Deletar", "Delete", new { id = usuario.Id }, new { @class = "btn btn-danger" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-danger">
            <p>Não há funcionários cadastrados.</p>
        </div>
    }
</div>