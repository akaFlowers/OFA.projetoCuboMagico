@model IEnumerable<projetoCuboMagico.Models.GeneroLivro>

@{
    ViewData["Title"] = "Usuarios";
}

<div class="page-header">
    <h2>@ViewData["Title"]</h2>
</div>
<div style="padding-bottom: 15px;" class="text-right">
    @Html.ActionLink("Cadastrar", "Create", null, new { @class = "btn btn-success" })
</div>
<div class="table-responsive">
    @if (Model.Any())
    {
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(g => g.Id)</th>
                    <th>@Html.DisplayNameFor(g => g.GeneroLivroo)</th>
                    <th>@Html.DisplayNameFor(g => g.SubGenero)</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var generoLivro in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(g => generoLivro.Id)</td>
                        <td>@Html.DisplayFor(g => generoLivro.GeneroLivroo)</td>
                        <td>@Html.DisplayFor(g => generoLivro.SubGenero)</td>
                        <td>
                            @Html.ActionLink("Detalhes", "Details", new { id = generoLivro.Id }, new { @class = "btn btn-info" })
                            @Html.ActionLink("Editar", "Edit", new { id = generoLivro.Id }, new { @class = "btn btn-warning" })
                            @Html.ActionLink("Deletar", "Delete", new { id = generoLivro.Id }, new { @class = "btn btn-danger" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-danger">
            <p>Não há funcionários cadastrados.</p>
        </div>
    }
</div>